# 我做了一個多平台內容採集框架——7 平台全覆蓋，開源免費

## 為什麼又双叒叕造輪子

做數據分析、內容研究、輿情監控的時候，經常需要從多個平台採集公開數據。

市面方案幾個痛點：只支援單一平台，換平台就得換工具；強依賴 Node.js（Puppeteer），對 Python 技術棧不友好；輸出格式單一，只給你 CSV；License 含糊——MIT 掛著，商用風險你根本不知道。

於是有咗 **Smart Agent**——一個純 Python、模組化、7 平台全覆蓋的內容採集框架。

> GitHub 地址：https://github.com/Smart75850/smart-agent

## Smart Agent 是什麼

一句話：**7 平台 × 全覆蓋 × 瀏覽器模式**的內容採集矩陣。

| 維度 | 支援 |
|------|------|
| 平台 | B站、抖音、小紅書、知乎、快手、微博、貼吧 |
| 操作 | 關鍵詞搜索、排行榜/熱榜、內容詳情、評論採集、用戶作品 |
| 儲存 | JSON、CSV、JSONL、Excel、SQLite、MySQL |
| 引擎 | Playwright 瀏覽器自動化 + CDP 模式（復用已登錄 Chrome） |

## 核心亮點

### 1. 零 Node.js 依賴，純 Python

```bash
pip install -r requirements.txt
playwright install chromium
```

不需要裝 Node、不需要配 Puppeteer、不需要折騰 npm。Python 3.11+ 即可。

### 2. CDP 模式——復用已登錄的瀏覽器

這是我覺得最實用的功能。抖音、小紅書等平台需要登錄才能拿到完整數據。傳統做法是腳本內置 Cookie，但 Cookie 幾天就過期，每週都得手動更新。

CDP 模式的做法優雅得多：

```bash
# 1. 正常打開 Chrome，手動登錄各平台
chrome --remote-debugging-port=9222

# 2. 腳本通過 CDP 協議直接連接到你已登錄的 Chrome
python main.py --platform douyin --keyword AI繪畫 --engine cdp
python main.py --platform xiaohongshu --keyword 穿搭 --engine cdp
```

腳本直接復用你瀏覽器的登錄態——不用管 Cookie、不用擔心過期、不用反覆登錄。一次登錄，長期使用。

### 3. WebUI 儀表板——零 npm 的暗色主題 SPA

```bash
python -m api.main
# → http://localhost:8000
```

打開就是暗色主題儀表板。選平台、填關鍵詞、看實時日誌（WebSocket 推送）、下載結果（JSON/CSV），全程滑鼠操作。整個前端是 **Vanilla JS 單頁應用**——沒有 npm、沒有 webpack、沒有 node_modules，一個 HTML 文件搞定。

### 4. 6 種儲存後端，一鍵切換

```bash
# JSON（默認輸出）
python main.py --platform bilibili --keyword Python

# CSV（utf-8-sig 編碼，Excel 直接打開不亂碼）
set STORE_BACKEND=csv
python main.py --platform bilibili --keyword Python

# SQLite
set STORE_BACKEND=sqlite
python main.py --platform all --keyword AI工具
```

JSON、CSV、JSONL、Excel、SQLite、MySQL 全部支援。所有後端共用同一個接口，切換只改一個環境變數。

### 5. Shadow DOM 穿透——小紅書評論區不再是黑洞

小紅書等平台的評論區使用 Shadow DOM 阻止自動化工具定位元素。Smart Agent 內置了 Shadow DOM 穿透邏輯，可以正常提取評論數據。這個能力在同類開源項目中不多見。

## Pro 版——純 HTTP 直連 + AI Agent + Go

開源版全部 7 平台都是**瀏覽器模式**，簡單穩定。但如果你需要更高性能，Pro 版提供：

**純 HTTP 直連**：全部 7 平台繞過瀏覽器，直接發 HTTP 請求。平均延遲從 3-4 秒降到 ~500ms，不需開瀏覽器，伺服器可部署。

**7 個 AI Agent**：
- 🔥 爆款識別——掃描全平台內容，自動識別高傳播潛力作品
- 📦 選品分析——從海量內容中挖掘可帶貨商品，給出變現潛力評分
- 🎬 視頻拆解——拆解爆款視頻結構：鉤子類型、節奏、轉化點
- 💬 評論情緒——分析評論區真實反應，判斷賽道真需求
- ✍️ 營銷文案——自動生成適配不同平台的營銷文案
- 🔄 內容改寫——跨平台適配：B站轉小紅書、知乎轉抖音
- 🖼️ 配圖策略——AI 生成封面設計策略+英文繪畫提示詞

**Go 高性能版**：6.7MB 單文件二進制，零依賴，雙擊即用。Windows / Mac / Linux 全平台支援。

**完整源碼交付**：一次性交付，包含所有 Python + Go + AI Agent 源碼。

## 快速上手

```bash
git clone https://github.com/Smart75850/smart-agent.git
cd smart-agent
pip install -r requirements.txt
playwright install chromium

# 開 Chrome CDP 模式（支援已登錄狀態）
chrome --remote-debugging-port=9222

# B站搜索
python main.py --platform bilibili --keyword Python

# 知乎熱榜
python main.py --platform zhihu --type hot

# 抖音搜索
python main.py --platform douyin --keyword AI繪畫 --engine cdp

# 全平台並發搜索
python main.py --platform all --keyword 美食
```

## 踩坑記錄

### Playwright vs Selenium 的選型

一開始用 Selenium，踩了幾個坑：瀏覽器啟動慢、CDP 模式支援不完善。切到 Playwright 後啟動速度明顯快了一截，而且原生支援 CDP 連接（`browser_type.connect_over_cdp`），代碼量少了至少 30%。

### 小紅書 Shadow DOM 穿透

小紅書評論區被 Shadow DOM 封裝了，普通的 `query_selector` 根本定位不到內部元素。解決思路是用 `page.evaluate` 執行 JS 穿透 Shadow Root，拿到內部 DOM 再處理。不同頁面的 Shadow DOM 層級還不一樣——搜索頁兩層，詳情頁三層。最後寫了一套遞歸穿透邏輯才穩定下來。

### Windows GBK 編碼問題

Windows 控制台默認編碼是 GBK，輸出 emoji（如 ✅ ❌ ⚠️）就亂碼。排查了兩小時後發現一行代碼就能解決：

```python
sys.stdout.reconfigure(encoding='utf-8', errors='replace')
```

## 最後

這個項目斷斷續續寫了幾個月。初衷是自己做數據分析和內容研究時需要一套趁手的工具——換一個平台就換一套代碼實在太煩了。

如果你也在做類似的事情，或者對爬蟲框架的設計有興趣，歡迎 Star、提 Issue、提 PR。

> GitHub：https://github.com/Smart75850/smart-agent

---

**⚠️ 合規提醒**：本項目僅供個人學習與學術研究使用。請遵守各平台的 robots 協議與用戶條款，合法合規使用。嚴禁用於任何商業用途或大規模數據抓取。詳見[完整免責聲明](https://github.com/Smart75850/smart-agent/blob/main/DISCLAIMER.md)。
