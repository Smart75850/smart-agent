# 开源一个月，用户帮我测出 15 个 Bug，是一种什么体验？

先说结论：第一反应是羞愧，第二反应是庆幸。

羞愧是因为「这代码居然是我写的」。庆幸是因为「幸好有人帮我测了，不然再多一个月我也发现不了」。

## 项目的来由

我写了一个叫 Smart Agent 的开源项目，纯 Python，用来跨平台采集和分析内容数据。支持 B站、抖音、小红书、知乎、快手、微博、贴吧七个平台，内置了七个 AI Agent，接上本地 Ollama 就能自动做爆款分析、选品挖掘、评论情绪之类的活。

自己开发和内测的时候觉得挺稳的，每个功能都跑过，逻辑也没什么问题。然后第一个用户来了。

然后我就知道什么叫「你觉得没问题是你的问题」。

## 用户的电脑和你的电脑，是两个世界

我自己测试的环境：Windows PowerShell、CDP Chrome 已登录小红书、网络畅通、API 拦截每次都成功、LLM Key 配好了。

用户的电脑：Windows cmd（不是 PowerShell）、CDP Chrome 开了但没登录、自己改过 Chrome 安装路径、Anaconda Python 3.9 环境。

两天之内他报了快十个问题。下面说几个我印象最深的。

## 第一个：环境变量被静默覆盖

用户设了 `set BROWSER_ENGINE=cdp`，想让程序连接他已经登录的 Chrome。但每次运行程序都会弹出一个全新的 Chrome 窗口。

查了半天发现 `main.py` 里有一行 `os.environ["BROWSER_ENGINE"] = args.engine`，而 `args.engine` 的默认值是 `playwright`。不管用户有没有设环境变量，这行代码都会用默认值覆盖掉。

我的第一反应是「操，这也行」。修复就一行代码的事，但用户因为这个折腾了快一个小时。

## 第二个：括号引发的血案

`browser.is_running` 是个 `@property`，返回布尔值。但兜底搜索函数里写成了 `browser.is_running()`，多了括号。Python 尝试把 `True` 当函数调。

这个 Bug 我自己永远测不出来，因为我跑测试的时候主路径一定是成功的，根本走不到那个兜底函数。用户一跑就踩到了。

这件事让我意识到，写代码的时候如果某个分支"觉得自己不会跑到"，那恰恰是最需要测试的分支。因为用户一定会跑到。

## 第三个：搜到了结果，保存的时候崩了

这个最尴尬。

用户搜小红书拿到了 40 条结果，然后在保存输出的时候程序崩了。报错：`UnboundLocalError: cannot access local variable 'settings'`。

原因是我之前在 `main()` 函数里的一个 `if` 分支里加了一行 `from config.settings import settings`。Python 的规则是函数内任何位置有 import 或者赋值，整个函数都会把那个名字当局部变量。用户没走到那个 `if` 分支，`settings` 从未被局部赋值，但 Python 仍然认为它是局部变量，后面用到 `settings.STORE_BACKEND` 的时候就炸了。

「我自己」跑的时候一定会走那个分支，所以完全没发现。写完这行代码的时候我还觉得「嗯这里需要 import 一下没问题」，现在回头看简直想抽自己。

## 第四个：正文只返回了 13 个字

用户拉取帖子详情，`desc` 字段只返回了「发现RED直播发布通知我我」。正文完全不匹配。

我一开始以为是选择器写错了，改了几版都不行。后来打开浏览器 DevTools 才发现，小红书把真正的帖子正文藏在 `window.__INITIAL_STATE__` 这个 SSR 内嵌的 JavaScript 变量里，752 字的正文、图片列表、标签、发布时间、IP 属地，全在里面。DOM 上的那个只是一个残缺的预览。

这是我作为一个 Python 开发者容易忽略的事情。现代前端框架的数据经常不在 HTML 里，在 JS 变量里。写爬虫之前应该先在 Console 里敲一句 `window.__INITIAL_STATE__`，看看数据到底在哪。

## 第五个：兜底路径的数据和主路径对不上

搜索用 API 拦截能拿到 15 个字段，热榜用 DOM 兜底只能拿到 4 个。用户从搜索结果能正常看详情，从热榜结果就不行，因为缺了 `xsec_token`。他问我「这不是同一个功能吗，为什么返回的东西不一样」。

确实。兜底的时候我只求不崩，没考虑数据格式要对齐。但用户不关心什么路径是主什么路径是兜底，在他眼里都是同一个功能。

## 说真的，开源对我意味着什么

那个用户没付我一分钱，但他帮我做了我花钱都买不到的事情：在完全陌生的环境里，用我完全没想到的方式，把我的代码从头到尾跑了一遍。他报的每一个 Bug 都是对一个盲区的指正。

所以我现在给自己定了几条规矩：

写代码的时候，至少测三条失败路径。环境变量没设会怎样、API 失败走兜底会怎样、没登录会怎样。你的环境完美不等于用户的环境完美，这句话写一百遍都不为过。

兜底路径的返回格式必须和主路径对齐。主路径有什么字段，兜底就要有什么字段，值可以为空，但 key 不能缺。

不要在一个 if 分支里局部 import 已经全局导入过的模块。Python 作用域规则有时候真的很反直觉。

认真回每一个 Issue。那是你花钱都买不到的 QA 团队。

这些规矩现在也适用我所有的项目。

## 项目地址

https://github.com/Smart75850/smart-agent

MIT 协议开源，欢迎 Star、Issue、PR。有问题看到就会回。
