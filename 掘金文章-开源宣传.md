# 我开源了一个多平台内容采集框架——5 平台 × 6 存储后端，纯 Python，一行命令开爬

## 痛点：为什么又要造轮子

做数据分析、舆情监控、学术研究的时候，经常需要从多个平台采集公开数据。

市面上的方案主要几个问题：要么只支持单一平台，换个平台就得换工具；要么强依赖 Node.js 生态（Puppeteer），对 Python 技术栈不友好；要么输出格式单一，只给你个 CSV；要么 License 含糊不清——MIT 协议挂着，实际商用风险你根本不知道。

更头疼的是需要登录的平台。小红书、抖音这些，每次跑脚本都得手动处理 Cookie，隔几天就过期，维护成本极高。

于是就有了 **Smart Agent**——一个纯 Python、模块化、多平台的内容采集框架。

> GitHub 地址：https://github.com/Smart75850/smart-agent

## Smart Agent 是什么

一句话概括：**5 个平台 × 5 种操作 × 6 种存储后端**的内容采集矩阵。

```
CLI 入口 → Platform Adapter → Browser Engine → Store 输出
```

| 维度 | 支持 |
|------|------|
| 平台 | B站、小红书、抖音、知乎、快手 |
| 操作 | 关键词搜索、排行榜/热榜、内容详情、评论爬取、用户作品 |
| 存储 | JSON、CSV、JSONL、Excel、SQLite、MySQL |
| 引擎 | Playwright（内置 Chromium）/ CDP 模式（复用已登录 Chrome） |

## 核心亮点

### 1. 零 Node.js 依赖，纯 Python 一把梭

```bash
pip install -r requirements.txt
playwright install chromium
```

不需要装 Node、不需要配 Puppeteer、不需要折腾 npm。Python 3.11+ 即可。

### 2. CDP 模式——复用已登录的浏览器，告别 Cookie 管理

这是我觉得最实用的功能。小红书、抖音、知乎、快手都需要登录才能拿到完整数据。传统的做法是脚本内置 Cookie，但 Cookie 几天就过期，每周都得手动更新一轮。

CDP 模式的做法优雅得多：

```bash
# 1. 正常打开 Chrome，手动登录各平台
chrome --remote-debugging-port=9222

# 2. 脚本通过 CDP 协议直接连接到你已经登录的 Chrome
python main.py --platform xiaohongshu --keyword AI绘画 --engine cdp
```

脚本直接复用你浏览器的登录态——不用输密码、不用管理 Cookie、不用担心过期。一次登录，长期使用。对需要同时操作多个登录平台的场景，这个方案省心太多。

### 3. WebUI 仪表板——零 npm 的暗色主题 SPA

```bash
python -m api.main
# → 浏览器打开 http://localhost:8000
```

打开就是一个暗色主题的仪表板。选平台、填关键词、看实时日志（WebSocket 推送）、下载结果（JSON/CSV），全程鼠标操作。

整个前端是 **Vanilla JS 单页应用**——没有 npm、没有 webpack、没有 node_modules，一个 HTML 文件搞定。加载速度秒杀那些 React 重框架搭的后台。

### 4. 6 种存储后端，一键切换

```bash
# JSON（默认输出）
python main.py --platform bilibili --keyword Python

# CSV（utf-8-sig 编码，Excel 直接打开不乱码）
set STORE_BACKEND=csv
python main.py --platform bilibili --keyword Python

# SQLite
set STORE_BACKEND=sqlite
python main.py --platform bilibili --keyword Python
```

JSON、CSV、JSONL、Excel（openpyxl）、SQLite、MySQL 全部支持。所有后端共用同一个接口，切换只改一个环境变量，不用改一行代码。

### 5. Shadow DOM 穿透——小红书的评论区不再是黑洞

小红书等平台的评论区使用了 Shadow DOM 来阻止自动化工具定位元素。Smart Agent 内置了 Shadow DOM 穿透逻辑，可以正常提取评论数据。这个能力在同类开源项目中不多见。

### 6. MCP Server——让 AI 帮你爬数据

内置了 MCP（Model Context Protocol）Server，可以接入 Claude Code 等 AI 工具，直接通过自然语言对话来发起爬取任务：

> "帮我把 B站 热搜前 10 的标题和播放量爬下来，输出 CSV"

这是目前市面上同类开源项目里独有的功能。

### 7. 许可清晰——学习可以，商用来谈

使用 **NON-COMMERCIAL LEARNING LICENSE 1.0**。学习、研究随你用；想商用，来谈授权。

## 快速上手

```bash
git clone https://github.com/Smart75850/smart-agent.git
cd smart-agent
pip install -r requirements.txt
playwright install chromium

# B站热搜榜
python main.py --platform bilibili --type rank

# 小红书搜索
python main.py --platform xiaohongshu --keyword AI绘画

# 知乎热榜
python main.py --platform zhihu --type hot

# 抖音用户作品
python main.py --platform douyin --user_id xxx

# 全平台搜索（同时跑 5 个平台）
python main.py --platform all --keyword Python
```

## 项目结构

```
smart-agent/
├── main.py                     # CLI 入口
├── api/                        # WebUI（FastAPI + WebSocket + Vanilla SPA）
│   ├── main.py                 # FastAPI 应用
│   ├── routers/                # API 端点
│   └── webui/
│       └── index.html          # 暗色主题单页应用
├── config/
│   └── settings.py             # 集中配置（dataclass + 环境变量）
├── base/
│   └── platform_base.py        # PlatformAdapter 抽象基类
├── src/
│   ├── agents/                 # 5 个平台的 Adapter 实现
│   └── mcp_tools/              # MCP Server
├── store/                      # 6 种存储后端
├── proxy/
│   └── proxy_manager.py        # 轮询代理池
├── cache/
│   └── memory_cache.py         # TTL 内存缓存
└── tests/
```

## 踩坑记录

### Playwright vs Selenium 的选型

一开始用 Selenium，踩了两个坑：一是浏览器启动速度慢，冷启动要 3-5 秒；二是 CDP 模式支持不完善，得自己封装一大坨代码。切到 Playwright 后启动速度明显快了一截，而且原生支持 CDP 连接（`browser_type.connect_over_cdp`），代码量少了至少 30%。

### CDP 连接断开问题

复用 Chrome 登录态的时，偶尔遇到连接断开——发现是 Chrome 的 `--remote-debugging-port` 在某些网络环境下被防火墙拦截。解决方案：启动 Chrome 时加上 `--disable-features=TranslateUI` 等参数来减少不必要的网络请求，同时给 CDP 连接加了一套自动重试逻辑，断了自动重连。

### 小红书 Shadow DOM 穿透

小红书的评论区被 Shadow DOM 封装了，普通的 `query_selector` 根本定位不到内部元素。解决思路是用 `page.evaluate` 执行 JS 穿透 Shadow Root，拿到内部 DOM 再处理。但不同帖子页面的 Shadow DOM 层级还不一样——搜索页是两层，详情页是三层。最后写了一套递归穿透逻辑才稳定下来。

### Windows GBK 编码——排查两小时，修复一行代码

Windows 控制台默认编码是 GBK，输出 emoji（比如日志里的 ✅ ❌ ⚠️）会乱码。排查了两个小时后发现一行代码就能解决：

```python
sys.stdout.reconfigure(encoding='utf-8', errors='replace')
```

## 路线图

- [ ] 微博、贴吧平台支持
- [ ] 断点续爬
- [ ] Docker 一键部署
- [ ] Pro 版（更多平台、更强反反爬能力）

## 最后

这个项目断断续续写了两个多月。初衷是自己做数据分析和内容研究时需要一套趁手的工具——换一个平台就得换一套代码实在太烦了。

如果你也在做类似的事情，或者对爬虫框架的设计有兴趣，欢迎来 Star、提 Issue、提 PR。

> GitHub：https://github.com/Smart75850/smart-agent

---

**⚠️ 合规提醒**：本项目仅供个人学习与学术研究使用。请遵守各平台的 robots 协议与用户条款，合法合规使用。严禁用于任何商业用途或大规模数据抓取。触碰法律红线，后果自负。详见 [完整免责声明](https://github.com/Smart75850/smart-agent/blob/master/DISCLAIMER.md)。
