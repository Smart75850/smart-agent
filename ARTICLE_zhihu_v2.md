# 开源一个月，用户帮我测出 15 个 Bug——是一种什么体验？

> 从「我自己试过能跑啊」到「原来用户是这样用的」。

---

写代码的人都知道一种感觉：**在自己电脑上跑得通，用户一用就崩。**

我的开源项目 Smart Agent 上线 GitHub 快一个月了。这是一个纯 Python 的多平台内容采集框架，支持 B站、抖音、小红书等 7 个平台，还内置了 7 个 AI Agent 做内容分析。

开发和自测花了很长时间，每个功能都反复跑过，觉得自己写得很稳。然后第一个用户来了。然后 Bug 报告来了。然后我发现了一个残酷的事实——

**我做的叫"演示"，用户做的才叫"测试"。**

---

## 用户的电脑和你的电脑，是完全不同的两个世界

我在 Windows PowerShell 里配置环境、CDP Chrome 已经登录、网络畅通、Python 3.14——一切完美。

用户的电脑是：Windows cmd（不是 PowerShell）、CDP Chrome 开了但没登录小红书、自己改过 Chrome 安装路径、Python 3.9 的 Anaconda 环境。

48 小时内，他报了好几个 Bug。每一条我都觉得「怎么可能？」——然后定位到代码，发现真的就是我的锅。

---

## 五个让我汗颜的 Bug

### 一、环境变量被无声覆盖

用户设了 `set BROWSER_ENGINE=cdp`，想让程序连接他已经登录的 Chrome。但程序弹出了一个全新的 Chrome 窗口。

原因：`main.py` 里有一行 `os.environ["BROWSER_ENGINE"] = args.engine`，而 `args.engine` 的默认值是 `playwright`。用户的环境变量被默默覆盖了。

**一个参数默认值，浪费了用户一个小时。** 这是我的责任，不是用户的问题。

### 二、一个括号引发的崩溃

`browser.is_running` 是一个 `@property`（属性），返回 `True` 或 `False`。但在兜底搜索函数里，有人写了 `browser.is_running()`——多了括号。Python 尝试把 `True` 当成函数调用，于是 `True()` 炸了。

**这种 Bug 在"自测"阶段永远触发不了**——因为我自己跑的时候，根本不会走到兜底路径。

### 三、搜到了数据，保存时崩了

用户搜小红书拿到了 40 条结果，然后在保存输出时程序崩了。报错：`UnboundLocalError: cannot access local variable 'settings'`。

原因：我在 `if args.type == "aggregate"` 分支里写了一个局部 `from config.settings import settings`。Python 的规则是——函数内任何位置有 `import` 或赋值，整个函数都会把那个名字当局部变量。用户没走这个分支，`settings` 从未被局部赋值，但 Python 仍然认为它是局部变量。

**这个 Bug 我自测时永远不会踩到**——因为我总是用 `--type aggregate` 跑。

### 四、正文只返回了 13 个字

用户拉取帖子详情，`desc` 字段只返回了「发现RED直播发布通知我我」。正文完全不匹配。

原因是：我写的是 CSS 选择器刮 DOM，但小红书把真正的正文藏在 `window.__INITIAL_STATE__`——一个 SSR 内嵌的 JavaScript 变量里，里面有标题、752 字正文、图片列表、标签、发布时间、IP 属地。DOM 上只有一个残缺的预览。

**现代前端框架的数据不在 HTML 里，在 JS 变量里。** 这是我作为一个 Python 开发者容易忽略的。

### 五、搜索和热榜返回的字段不一样

搜索用 API 拦截拿 15 个字段，热榜用 DOM 兜底只拿 4 个字段。用户从搜索结果能正常看详情，从热榜结果就不行——因为缺 `xsec_token`。

兜底路径我只求「不崩溃」，没求「对齐主路径」。但用户不知道什么路径是主什么路径是兜底——对他来说，都是同一个功能，居然返回不一样的数据，那就是 Bug。

---

## 六个我学到的教训

回到标题的问题：开源一个月，用户帮你测出 15 个 Bug 是什么体验？

**第一反应是羞愧**——「这代码真的是我写的？」
**第二反应是庆幸**——幸好有人测了，不然再多一个月我也发现不了
**第三反应是行动**——把这 6 条写进了我的开发规范：

1. **环境变量优先于 CLI 默认值**——用户显式设的，不要覆盖
2. **`@property` 不加括号，普通方法加括号**——混淆了就等着 `bool not callable`
3. **不在 if 分支里 import 已导入的模块**——Python 作用域规则很反直觉
4. **现代 SPA 的数据在 `window.__INITIAL_STATE__` 里**——不要只信 DOM
5. **兜底路径返回的数据结构必须和主路径一致**
6. **每次都测 Failure Path，不要只跑 Happy Path**

---

## 开源到底值不值得？

有人问我：花这么多时间修 Bug、回 Issue、写文档，值得吗？

**值得。** 理由很简单——如果没有这个用户，我永远不会知道我的代码在真实环境下有这么多问题。而且这些教训不仅适用于这个项目，它们会贯穿我以后写的每一行代码。

那个用户虽然没付我一分钱，但他帮我做了我花钱都买不到的事情：**在完全陌生的环境里，用我完全没想到的方式，把我的代码从头到尾跑了一遍。**

所以如果你也在做开源项目，我的建议是：

- **认真回每一个 Issue，那是免费的 QA**
- **写一个懒人包脚本，用户双击就能跑**
- **不要假设用户的环境和你一样**
- **每一个 Bug 都是一篇好文章**

---

*项目地址：https://github.com/Smart75850/smart-agent*

*如果你觉得这篇文章有用，欢迎点赞收藏。如果你对多平台内容采集、Python 爬虫、AI Agent 感兴趣，也欢迎来 GitHub 提 Issue 或 PR。*
